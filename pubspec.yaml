name: dbro_timein_app
description: "D'Bro Employee Time In"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  qr_code_scanner_plus: ^2.0.10+1
  url_launcher: ^6.3.2
  intl: ^0.19.0
  mailer: ^6.4.2
  flutter_local_notifications: ^19.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
